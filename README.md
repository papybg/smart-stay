Това е структуриран и професионален **README.md** файл, предназначен за друг изкуствен интелект (или разработчик). Той описва архитектурата, текущото състояние и бъдещите стъпки на проекта **Smart Stay**.

---

# 🏨 Smart Stay Agent: Система за Автоматизация на Краткосрочни Наеми

### 📝 Описание
### 📝 Описание (Статус: Януари 2026)

Проектът представлява интелигентна система за управление на апартаменти под наем (тип Airbnb). Системата интегрира изкуствен интелект (Gemini 3), смарт хардуер (Tuya IoT) и облачна база данни (Neon PostgreSQL) за автоматизиране на комуникацията с гостите и контрол на енергията.

---

### 🤖 Архитектура на Тримата Агента

Системата функционира като съвкупност от три специализирани агента:

1.  **Резервационен Агент:**
    *   Приема резервациите (автоматично от Airbnb или ръчно въведени през Админ панела).
    *   Записва данните в Neon DB и следи статуса на плащанията.

2.  **Управляващ Агент (Техник):**
    *   Управлява физическия достъп и средата (Ток и Брава).
    *   Автоматично включване/изключване на уредите спрямо часовете на настаняване.

3.  **Дигитален Иконом (Concierge):**
    *   Комуникира с гостите на естествен език.
    *   Отговаря на въпроси за: Местонахождение, Оборудване на апартамента, Хранене, Транспорт и Туристически гид.

---

### 📂 Файлова Структура и Съдържание

#### 1. `server.js` (Backend - Node.js)

* **Технологии:** Express.js, `@tuya/tuya-connector-nodejs`, `@google/generative-ai`, `pg` (PostgreSQL), `node-cron`.
* **Съдържание:**
* **Автопилот:** Скрипт, който сканира базата данни на всеки 10 минути и пуска тока при предстояща резервация (прозорец: 2-6 часа от текущия момент).
* **Автопилот (Smart Cron):** Сканира базата на всеки 10 минути. Следи не само бъдещи, но и активни резервации. Записва точния час на включване/изключване в базата (`power_on_time`), за да избегне дублиране на команди.
* **Smart Caching:** Кешира статуса на Tuya устройството за 30 секунди, за да предотврати API Rate Limiting при чести заявки.
* **AI Engine:** Конфигурация на **Gemini 3 Flash Preview** със системни инструкции за проверка на кодове и любезен отговор.
* **API Endpoints:** `/chat`, `/toggle`, `/status`, `/add-booking`, `/bookings`.
* **API Endpoints:** `/chat`, `/toggle`, `/status`, `/add-booking`, `/bookings`, `/update-db`.
* **Сигурност:** Basic Authentication (Login) за защита на административните пътища.
* **CORS:** Настроен да приема заявки от `stay.bgm-design.com` и localhost.


* **Брой редове:** ~142.

#### 2. `admin.html` (Административен Панел)

* **Функция:** Интерфейс за собственика.
* **Съдържание:** Форма за въвеждане на име, `datetime-local` за вход/изход и код за резервация. Таблица, показваща реалните записи от Neon DB с автоматично генерираните 6-цифрени ПИН кодове.
* **Брой редове:** ~85.
* **Съдържание:** 
    *   Форма за резервации с автоматично конвертиране към UTC (ISO String).
    *   Таблица с цветови индикатори за статус на тока (⚡ ВКЛЮЧЕН / 🌑 ИЗКЛЮЧЕН) и точно време на събитието.
    *   "Live Status" индикатор за моментното състояние на апартамента.
    *   Филтър за скриване на резервации, приключили преди повече от 24 часа.

#### 3. `remote.html` (Дистанционно Управление)

* **Функция:** Ръчен контрол на смарт електромера.
* **Съдържание:** Интелигентен бутон със статус в реално време. Използва `setInterval` (3 сек), за да синхронизира визуалното състояние (ON/OFF) директно от данните в Tuya Cloud.
* **Брой редове:** ~85.
* **Съдържание:** Интелигентен бутон със статус в реално време. Използва `setInterval` (10 сек) и кеширани данни от сървъра за мигновена реакция.

#### 4. `.env` (Конфигурация - Скрити променливи)
#### 4. `index.html` (Guest Concierge UI)

* Съдържа: `GEMINI_API_KEY`, `TUYA_ACCESS_ID`, `TUYA_ACCESS_SECRET`, `TUYA_DEVICE_ID`, `DATABASE_URL` (Neon).
* **Функция:** Чат интерфейс за гостите (Дигитален Иконом).
* **Дизайн:** Модерен "Messenger-like" UI, оптимизиран за мобилни устройства (100dvh).
* **Функционалност:** Изпраща съобщения към AI агента (`/chat`) и визуализира отговорите.

#### 5. `.env` (Конфигурация - Скрити променливи)

* Съдържа: `GEMINI_API_KEY`, `TUYA_ACCESS_ID`, `TUYA_ACCESS_SECRET`, `TUYA_DEVICE_ID`, `DATABASE_URL`, `ADMIN_USER`, `ADMIN_PASS`, `AIRBNB_ICAL_URL`.

---

### ✅ Какво е преминато (Завършени етапи)

* [x] Интеграция на Tuya Cloud API (V1.0) със система за подписване (Signature).
* [x] Настройка на Neon PostgreSQL база данни с таблица `bookings`.
* [x] Свързване на Gemini 3 Flash Preview с логика за "Function Calling" (чрез текстови маркери като `CHECK_CODE`).
* [x] Корекция на часовите зони (UTC vs EET) за прецизна автоматизация.
* [x] Създаване на Remote панел с реален статус.
* [x] **Сигурност:** Добавена защита с парола за Admin и Remote панелите.
* [x] **Админ функции:** Валидация на дати, защита от дублиране и изтриване на резервации.
* [x] **История и Статус:** Добавено проследяване на включване/изключване в базата данни и визуализация в админ панела.
* [x] **Оптимизация:** Кеширане на заявки и увеличен интервал на опресняване.

---

### 🛠 Какво остава да се направи (Бъдещи етапи)

1. **Интеграция на Смарт Брава:** Свързване на ПИН кода от базата данни с реална брава (TTLock или Tuya Lock API).
2. **Автоматично изпращане на съобщения:** Интеграция с WhatsApp/Viber API за автоматично изпращане на ПИН кода към госта при плащане.
3. **Енергиен отчет:** Визуализация на консумираната енергия от електромера в реално време в Remote панела.
4. **Сигурност:** Добавяне на аутентикация (Login) за `admin.html` и `remote.html`.

---

### ⚠️ Технически бележки за следващия AI

* **Модели:** Да се използват приоритетно **Gemini 3 Flash Preview** или **Gemini 3 Pro Preview**.
* **База данни:**Neon DB работи със SSL връзка.
* **Хардуер:** Tuya сървърът е базиран в Европа (`https://openapi.tuyaeu.com`).

---

**Мога ли да ти помогна с нещо друго по документацията или си готов за следващата стъпка?**